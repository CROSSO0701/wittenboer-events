node_modules
.next
out
.DS_Store
.env
.env.local
.env*.local
*.log
.vercel
.claude/
.claude-flow/
*-PROMPT.md
*-prompt.md
tsconfig.tsbuildinfo
supabase/.temp/
*.tmp

# Internal sales docs (all formats + version variants) — never commit
Offerte-*

# Heavy media that does not belong in the repo
*.mp3
*.mp4
*.zip

# LibreOffice/OpenOffice lock files
.~lock.*#

# Loose dropped files / scratch assets in the repo root — keep them out of `git add .`
/pers fotos/
/pngtree-*
/worst-stamp-*.webp
/avb554hcd.webp
/https___cdn.pijper.io*.png
/Challenges-groep-nieuw.png
